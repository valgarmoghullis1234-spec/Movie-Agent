# Python
backend/.venv/
__pycache__/
*.pyc

# Env / secrets
.env
.env.local
backend/.env
frontend/.env.local

# Node / Next
node_modules/
frontend/.next/
frontend/out/

# Local npm cache (workaround for broken global cache)
.npm-cache/

# Local editor / agent settings (machine-specific)
.claude/settings.local.json

# OS
.DS_Store
